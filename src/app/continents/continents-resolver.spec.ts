import { continentsResolver } from './continents-resolver';

describe('ContinentsResolver', () => {
  it('should create an instance', () => {
    expect(new continentsResolver()).toBeTruthy();
  });
});
